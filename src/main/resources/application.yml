spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.34.178.13:3306/ANITIME?serverTimezone=Asia/Seoul
    username: kym
    password: kym123

  # springfox-swagger(?? 3.0.0)? spring-boot(2.6.2) ???? ??
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  # jpa ??
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql

